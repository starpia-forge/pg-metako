# Purpose    : Example environment variables for Docker deployment
# Context    : Template for configuring pg-metako Docker environment
# Constraints: Must provide secure defaults and clear documentation

# PostgreSQL Database Configuration
POSTGRES_DB=myapp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_password

# PostgreSQL Replication Configuration
POSTGRES_REPLICATION_USER=replicator
POSTGRES_REPLICATION_PASSWORD=change_this_replication_password

# PostgreSQL Port Configuration
POSTGRES_MASTER_PORT=5432
POSTGRES_SLAVE1_PORT=5433
POSTGRES_SLAVE2_PORT=5434

# pg-metako Application Configuration
PG_METAKO_PORT=8080
LOG_LEVEL=info

# pgAdmin Configuration (optional)
PGADMIN_EMAIL=admin@pgmetako.local
PGADMIN_PASSWORD=change_this_admin_password
PGADMIN_PORT=8081

# Docker Compose Profiles
# Uncomment to enable pgAdmin
# COMPOSE_PROFILES=admin